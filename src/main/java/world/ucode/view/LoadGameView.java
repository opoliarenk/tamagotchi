package world.ucode.view;

import javafx.stage.Stage;

public class LoadGameView extends View {
    public LoadGameView(String fxml, Object controller, Stage stage) {
        super(fxml, controller, stage);
    }
}
